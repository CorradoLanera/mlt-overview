type: append
slug: 00-setup
title: "Step 00 — Setup"
minutes: 25
summary: "Initialise a reproducible RStudio project from scratch with `renv` and `here`."
packages: [here, rio]
check:
  kw: [panel-tabset, To fill, Solved, dim]
  imgs: 0
